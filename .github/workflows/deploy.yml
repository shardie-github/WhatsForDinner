# ⚠️ DEPRECATED: This workflow is deprecated and will be removed.
# 
# Use these workflows instead:
# - `.github/workflows/frontend-deploy.yml` - Frontend deployments to Vercel (preview + production)
# - `.github/workflows/supabase-migrate.yml` - Database migrations (separate concern)
#
# This workflow mixed frontend deployment with database migrations, which violates
# separation of concerns. Frontend deployments and database migrations should be
# decoupled and run independently.
#
# This workflow is disabled (no triggers) and kept for reference only.
# It will be removed in a future cleanup.
#
# REMOVAL DATE: 2025-02-28 (30 days from deprecation)

name: Deploy (DEPRECATED - REMOVE BY 2025-02-28)

# Disabled triggers - workflow will not run
# on:
#   push:
#     branches: [main]
#   workflow_dispatch:

env:
  NODE_VERSION: '20'
  PNPM_VERSION: 9

jobs:
  deploy-production:
    name: Deploy to Production (DEPRECATED)
    runs-on: ubuntu-latest
    environment: production
    concurrency:
      group: prod-deploy
      cancel-in-progress: false
    env:
      NEXT_TELEMETRY_DISABLED: 1
      NEXT_PUBLIC_SITE_URL: https://${{ secrets.VERCEL_PROJECT_DOMAIN }}
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Next.js
        run: pnpm run build:web

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1

      - name: Supabase Login & Link
        run: |
          pnpm run supa:login || echo "Supabase login skipped if token not available"
          pnpm run supa:link || echo "Supabase link skipped if not configured"
        continue-on-error: true

      - name: Apply Supabase Migrations (Prod)
        run: |
          pnpm run supa:migrate:apply || echo "Migrations skipped if not configured"
        continue-on-error: true

      - name: Vercel Pull & Build
        run: |
          pnpm run vercel:pull || echo "Vercel pull skipped if not configured"
          pnpm run vercel:build || echo "Vercel build skipped if not configured"
        continue-on-error: true

      - name: Deploy to Vercel (Production)
        run: pnpm run vercel:deploy:prod || echo "Vercel deploy skipped if not configured"
        continue-on-error: true
